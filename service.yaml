apiVersion: v1
kind: Service
metadata:
  name: transitx-api-dev
  namespace: transitx-dev
spec:
  type: LoadBalancer
  selector:
    app: transitx-dev-backend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080